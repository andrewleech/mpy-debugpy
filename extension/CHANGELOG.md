# Changelog

## 0.1.0

First preview release:

- a `micropython` launch configuration that runs `mpremote debug` and
  attaches `ms-python.debugpy` to the endpoint the target reports;
- a status-bar picker for `mpdebug.toml` targets;
- `source` (mount) and `loop` (rerun on Restart) options.
