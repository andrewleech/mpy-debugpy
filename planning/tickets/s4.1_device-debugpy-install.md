# mpremote+wrapper: Ensure debugpy package present on device, cross-compiled and hash-cached

Phase: EPIC-4 / STORY-4.1
Depends on: STORY-2.1 (DONE — D2 conditional yes), mpremote transport with `verify_hash` (ampremote lineage; see Open questions)
Written: 2026-07-06 at HEAD 59fab8450a (micropython 18c76f7c56, micropython-lib 0d42fcd5bc; ampremote/micropython 5b459e2d57)

## Context

Mount is the mainline source-delivery mechanism (D2, `../SPIKE-2.1-mount.md`), so the
only thing that must physically live on the device filesystem is the debugpy package
itself (a mounted debugpy would add fs-RPC latency to every trace callback and dies with
the serial link). Today the old wrapper re-ran `compile_debugpy.py` + `mip install` +
`cp -r` on every run with no staleness detection. This ticket makes package install a
cached, verified, one-shot step: cross-compile to `.mpy`, install once, skip when
nothing changed.

Constraining decisions: D2 (mount mainline → this ticket is the ONLY unconditional part
of EPIC-4), D5 (canonical lineage), Q1 (mpremote work lives on one `andrewleech/micropython`
branch consumed by both ampremote and this repo).

## Scope

In scope: a reusable "ensure debugpy on target" step — cross-compile the debugpy package
sources to `.mpy`, compute a cache key, compare against an on-device marker, install only
on mismatch, verify each written file by device-side sha256.
Out of scope: syncing USER source (s4.2 fallback / mount mainline); attach-time staleness
of user source (s4.3); wiring into `do_debug` (s5.3 consumes this as a function).

## Files and anchors

- Package to install: `micropython-lib/python-ecosys/debugpy/debugpy/` — exactly 8
  modules: `__init__.py`, `public_api.py`, `common/{__init__,constants,messaging}.py`,
  `server/{__init__,debug_session,pdb_adapter}.py`. `manifest.py` at the package root is
  `package("debugpy")` — the module list can be derived by globbing the package dir, no
  hand-kept JSON manifest (the old repo's `debugpy.json`/`debugpy_mpy.json` pattern is
  what this replaces).
- mpy-cross build: `Makefile` target `mpy-cross` (`make -C micropython/mpy-cross`);
  binary lands at `micropython/mpy-cross/build/mpy-cross`. Version string for the cache
  key via `mpy-cross --version`.
- Hash-verified write: `fs_writefile(..., verify_hash=True)` exists ONLY in the
  ampremote lineage — `~/ampremote/micropython/tools/mpremote/mpremote/transport.py:292`
  (writefile) and `:194` (readfile). This repo's micropython submodule
  (`micropython/tools/mpremote/mpremote/transport.py:154`) has the plain
  `fs_writefile(dest, data, chunk_size=256, progress_callback=None)` — NO verify_hash.
  See Open questions.
- Existing hash primitive on device: upstream mpremote computes device-side sha256 via
  `hashlib` in the helper snippets ampremote's #18436 branch adds; reuse, don't reinvent.

## Design constraints

- Never trust names: the installed-package marker records content hashes, not versions.
- No behaviour change to the debugpy package itself here — install-side only.
- Cache key = (sha256 over sorted source files, mpy-cross version string, mpy-cross
  flags e.g. `-O2`, march if set). Any component changes → full reinstall.
- Serial-transport friendly: must work over raw REPL with nothing but `state.transport`
  primitives, since `do_debug` (s5.1) will call it before the session starts.
- Follow mpremote house style (extend existing primitives; new args default to old
  behaviour).

## Approach sketch

1. Host side: glob the package dir, cross-compile each `.py` → `.mpy` into a host cache
   dir keyed by the cache key (so switching firmware/flags doesn't thrash).
2. Compute the install manifest: `{device_path: sha256(mpy_bytes)}` + the cache key.
3. On device, read `:/lib/.debugpy-install.json` (or absent). If key matches, done —
   zero file transfers.
4. Else write each `.mpy` under `:/lib/debugpy/...` with `verify_hash=True`, then write
   the marker file last (so an interrupted install re-runs).
5. Expose as a function importable by the future `do_debug` (Q1 branch) and callable
   standalone for tests.

## Acceptance criteria and tests

- First run against a wiped device installs all modules; `import debugpy` succeeds on
  target.
- Second run with unchanged sources performs no `fs_writefile` calls (assert via
  transport call spy / DAP-less unit test).
- Touching one source file reinstalls (at minimum) that module and updates the marker.
- Changing mpy-cross version or flags forces full reinstall.
- A corrupted transfer (simulated hash mismatch) fails loudly, and the marker is NOT
  updated.
- Adversarial: kill the install mid-way; next run must detect the stale/absent marker
  and reinstall rather than assume success.

## Revalidation (2026-08-05, phase entry at HEAD ab55bb6)

Anchors hold on the bumped submodules (micropython `f9d7c96b96`,
micropython-lib `00d364e7fb`): the package is still exactly the 8 modules
listed, `manifest.py` is still `package("debugpy")`, this repo's
`fs_writefile` (now `transport.py:207`) still has no `verify_hash`, and
ampremote's lineage still carries it (`transport.py:194`/`:292`). Drift since
the ticket was written:

- Its first open question is closed by D6 → option (a), register the #18436
  branch (`mpremote_file_cp_hash` in ampremote's `mbm.toml`) into this repo's
  composition. That is the first execution step; drop the feature-detect
  fallback.
- Its second open question is closed by STORY-3.2 as executed: the CI builds
  do NOT freeze debugpy into device firmware, so there is no frozen copy to
  detect and the install path stays `:/lib/debugpy`.
- STORY-5.1's `mpremote_debug_command` branch is parked unregistered pending
  Q8, so this story develops on its own branch and registers independently;
  s5.3 consumes both.

## Execution progress

**Registration step DONE (2026-08-05, commit 700c311).** `#18436`
(`mpremote_file_cp_hash`) is registered in `mbm.toml` as the last micropython
entry and composed in: integration tip `f9d7c96b96` -> `f8852e8fb2ca`, pinned
recompose against `06bcfd5b74`, nothing outside `tools/mpremote` changed.
Gates: unix build clean, host suite 82 passed / 1 xfailed (one retry for the
allowlisted `test_epic1_readonly_locals_hint` flake). Side branches verified
back at canonical tips; previous pin preserved as tag
`mpy-debugpy-pin-f9d7c96b96a4`.

The rebase hit a real conflict worth recording, since it will recur on every
future recompose until #18436 is updated upstream. Upstream added
`_quote_path()` (`transport.py:101`) after #18436 was authored and applies it
to every path interpolated into a device-side exec string; #18436 still
builds those strings with unquoted `'%s'` in BOTH its `verify_hash` and plain
branches of `fs_readfile`/`fs_writefile`. Taking either side wholesale loses
something: theirs drops the quoting hardening, ours drops the feature. The
resolution threads `_quote_path()` through all four exec strings; rerere has
recorded it and replayed it successfully on a clean re-run. If #18436 is
rebased upstream, check this hunk rather than trusting the replay.

Operational note for future recomposes: resolving a mid-run conflict with
`git rebase --continue` (rather than the documented `git merge --continue`
path) leaves mbm's state machine believing that branch was handled, so
`--resume` skips merging it into `mpy-debugpy_update` and reports success
with one branch missing. Verify the composed tip actually contains each
registered branch's content before landing.

**Implementation IN REVIEW, not landed (2026-08-05).** `debugpy_install.py`
exists on `andrewleech/micropython` branch `mpremote_debugpy_install` @
`75245612f9` (one commit off `upstream/master`):
`ensure_debugpy_installed(transport, package_dir, *, mpy_cross=None,
mpy_cross_flags=("-O2",), cache_dir=None, device_dir="/lib/debugpy",
marker_path="/lib/.debugpy-install.json")`, taking a raw transport like
`mip._install_package` so it needs no argparse wiring. Modules are globbed;
the host `.mpy` cache lives under `platformdirs.user_cache_dir("mpremote")`
keyed by the cache key, so a hit skips both transfers and the mpy-cross run.
Tests (13) are parked with it on top-repo branch `s4.1-debugpy-install`.

Four review rounds did not converge. Remaining mustFix findings:

- The write-order safety claim is false. Both the docstring and the commit
  message assert that writing deepest-path-first with the root
  `__init__.mpy` last keeps a half-installed package non-importable.
  MicroPython imports a directory with no `__init__` as a namespace package
  — verified against the built unix firmware: with only
  `lib/debugpy/server/x.mpy` present, `import debugpy` succeeds. The
  ordering buys nothing, so partial-install detection has to rest entirely
  on the marker.
- Marker verification is existence-only. The recorded per-file sha256 values
  are never read back, so a file corrupted after a successful install (an
  overwriting `cp`, a truncated flash write) still passes the fast path and
  the session runs against corrupt bytecode with no diagnostic. The data to
  catch it is already in the marker.
- Invalidating the marker before the host-side compile means an mpy-cross
  failure or Ctrl-C destroys a good device install and forces a reinstall of
  identical bytes.

Also worth carrying: `_read_marker` returns whatever the JSON parses to, so a
marker containing `3` raises `AttributeError` instead of degrading to a
reinstall — the exact failure it exists to absorb; and the test file's
module-level import of `mpremote.debugpy_install` breaks collection for the
WHOLE suite when the module is not in the pinned submodule, so the test file
must never land ahead of the composition.

Cleanup performed after the final round overstepped: it had registered the
branch in `mbm.toml` and force-moved `mpy-debugpy` to a compose carrying an
obsolete revision of the installer, leaving the top-repo gitlink pointing at
an upstream-master commit with no debug support. The integration branch is
restored to `f8852e8fb2ca` (registration-only, gates verified) on both the
local clone and the fork, `mbm.toml` carries only `mpremote_file_cp_hash`,
and the feature branch was rebuilt as one clean commit off `upstream/master`
after mbm's force-move had stacked it on top of #18436. The discarded
compose is preserved as fork tag `wip-s4.1-premature-compose`.

**Review CLEAN, still parked (2026-08-05).** Two further rounds closed the
three mustFix findings and the substantive nits; the final adversarial pass
returned zero mustFix. Branches: `mpremote_debugpy_install` @ `dcd4fb702c`
(one commit off `upstream/master`), top-repo `s4.1-debugpy-install` @
`7b1160f` (41 tests).

What the rounds actually changed, beyond the three findings already recorded:

- The fast path no longer trusts existence. It hashes each recorded file
  device-side (`fs_hashfile`, which uses the device's `hashlib` rather than
  pulling files back), and rejects any file under `device_dir` the marker
  doesn't record — the marker's hashes cannot see an *extra* file, and a
  plain `<stem>.py` dropped in later (by `mip install debugpy`, say) shadows
  the installed `<stem>.mpy`, so the session would have run stale source
  silently.
- A sweep that cannot delete a stale file now raises instead of writing a
  success marker, which would otherwise have made every later run trust the
  shadowing file.
- `device_dir` is refused if the sweep could reach the marker or the
  filesystem root; `ensure_debugpy_installed(..., device_dir="/lib")` would
  have deleted everything under `/lib` it did not write.
- Compile happens before the marker is invalidated, so an mpy-cross failure
  cannot destroy a good device install.
- `mip._ensure_path_exists` is promoted to `Transport.fs_ensure_path_exists`
  (both callers need mkdir-p over a transport); the test file now uses
  `pytest.importorskip`, so it skips rather than breaking collection for the
  whole suite when the module isn't in the pinned submodule.

Five of the six new behaviour tests were confirmed to fail against the
unfixed module.

**Caveat on the test numbers, do not carry them forward.** The branch's suite
passes against a unix firmware built from the *composed* tip, not from this
branch's own pin — the pin is upstream/master lineage, whose
`mpconfigvariant.h` has no `MICROPY_PY_SYS_SETTRACE` and whose
`fs_writefile` has no `verify_hash`, so a clean checkout of the side branch
would collapse the DAP suite and `ensure_debugpy_installed` would `TypeError`
against a real transport. Only the fake transports hide it. Re-take the
numbers once `mpremote_debugpy_install` is composed into `mpy-debugpy`.

Landing sequence: register the branch in `mbm.toml` (last entry), recompose
against the pinned target, re-run the suite against the composed firmware,
then land the top-repo side. Deliberately not done yet — see the note above
about a fix round that did exactly this prematurely.

## Workflow shape

Runs inside the EPIC-4 phase workflow: implementation sonnet, tests haiku (unit tests
with a fake transport + one QEMU/unix integration pass), review + adversarial review
opus, looped.

## Open questions

- `verify_hash` is not on this repo's mpremote (only ampremote's #18436 branch). Per Q1
  the debug tooling develops on one `andrewleech/micropython` branch — decide whether to
  (a) register the #18436 branch into this repo's `mpy-debugpy` integration too, or
  (b) make verify_hash optional (fallback: write then `fs_hashfile`-style re-read
  compare). (a) is consistent with D5/Q1 and recommended.
- Install path `:/lib/debugpy` vs frozen-into-firmware for the CI builds (s3.2) — if
  s3.2 freezes debugpy into device firmware, this ticket's runtime probe should detect
  the frozen copy and skip install entirely.
