# mpremote: one handshake parser across all control planes (control/data-plane split)

Phase: EPIC-5 / STORY-5.4
Depends on: s5.1 (skeleton), s5.3 (unix flow — first consumer); D3/Q3 (serial DAP deferred)
Written: 2026-07-06 at HEAD 59fab8450a (micropython 18c76f7c56, micropython-lib 0d42fcd5bc; ampremote/micropython 5b459e2d57)

## Context

Every transport launches the same boot script and gets back the same
`MPDBG-READY {json}` line, but over a different byte stream: subprocess stdout
(unix), raw-REPL exec output (serial device), or serial output from a device that is
about to be reachable over the network (network device before/after it has an IP).
The design position mandates the split: the CONTROL plane carries the handshake, the
DATA plane carries DAP. Per D3/Q3 (2026-07-06) the v1 data plane is TCP only; the
serial DAP tunnel is deferred but the parser and endpoint types must not preclude it
(SPIKE-2.2 retains the framing design for later).

## Scope

In scope: a single handshake reader parameterised over a "line source" (unix
subprocess pipe; raw-REPL output stream via mpremote transport), returning a
validated endpoint+caps record; endpoint resolution rules (see constraints);
timeout/error semantics shared by all callers.

Out of scope: the transports themselves (s5.3 unix, s6.1/s6.2 device flows consume
this); the serial DAP tunnel data plane (deferred by Q3); mDNS or any discovery
other than the handshake.

## Files and anchors

- `launcher/mpy_launch_debugpy.py:89-90` — the emit side: "Exactly one MPDBG-READY
  line, valid JSON, nothing else on this line." Payload keys `host`, `port`, `caps`
  (caps keys asserted in `tests/test_epic1_foundations.py:64`: `settrace`,
  `save_names`, `set_local`, `f_back`).
- `launcher/mpy_launch_debugpy.py:34-54` — `_detect_host()`: reports the device's
  own DHCP address when `network` is available and connected; otherwise (unix
  always; unconfigured devices) returns `"0.0.0.0"`. **So `host: "0.0.0.0"` is a
  legitimate payload value meaning "I bound all interfaces but don't know my
  address" — it is a bind address, never a connectable endpoint.**
- `tests/test_epic1_foundations.py:30-54` — `_read_mpdbg_ready`: the
  accumulate-and-scan parse loop (non-blocking chunks, exactly-one-line assertion)
  this ticket generalises.
- `tools/mpremote/mpremote/commands.py:499-515` — `_do_execbuffer` / `do_exec`: how
  raw-REPL exec output streams back on a device; the serial line source wraps this.
- `tools/mpremote/mpremote/transport_serial.py:49` — `SerialTransport`; its
  `read_until` primitive is the house-style way to scan for the prefix on serial
  (`~/ampremote/CLAUDE.md`: prefer extending `read_until`/`exec` over new
  abstractions).

## Design constraints

- **Endpoint always comes from the device/subprocess, never from user config.**
- **Never emit a raw `0.0.0.0` endpoint.** Resolution rules by control plane:
  unix subprocess → `127.0.0.1`; serial/network device reporting `0.0.0.0` → the
  transport's known address if any, else a hard error telling the user the device
  has no network address (do not guess). A device reporting a real address is taken
  verbatim.
- Exactly-one-line contract: duplicate or zero `MPDBG-READY` lines within the
  timeout is an error (matches the emit-side guarantee and the existing test).
- Design for a future non-TCP data plane: the parsed record should carry an
  endpoint kind (tcp host/port now; a serial-channel descriptor later per
  SPIKE-2.2) rather than a bare `(host, port)` tuple baked through every caller.
- Human banner lines around the handshake (`Debugpy listening on ...`,
  `public_api.py:57`) are noise — never parsed, never depended on.

## Approach sketch

`_read_mpdbg_ready(read_chunk, timeout) -> DebugEndpoint` where `read_chunk` is a
callable returning available bytes/str ('' when none): the unix caller passes a
non-blocking-pipe reader, the serial caller passes a transport-backed reader.
Accumulate, scan for `MPDBG-READY ` at line start, `json.loads` the rest of that
line, validate schema (host str, port int, caps dict of bools), then apply the
endpoint-resolution rules above with the caller-supplied context (control-plane
kind + known device address). Return record: `{kind: "tcp", host, port, caps,
raw_host}` keeping the reported `raw_host` for diagnostics.

## Acceptance criteria and tests

- [ ] one parser implementation serves unix stdout and raw-REPL serial output
  (unit-test with synthetic chunked streams; integration via s5.3/s6.x flows).
- [ ] `host: "0.0.0.0"` from a unix subprocess resolves to `127.0.0.1`; from a
  device with no known address it is a hard, explanatory error; a real device
  address passes through verbatim. No code path ever hands `0.0.0.0` to a DAP
  client.
- [ ] zero lines (timeout) and two lines both error, with the captured stream tail
  in the message.
- [ ] schema violations (port not int, caps not dict, non-bool cap) error rather
  than propagate garbage.
- Adversarial: handshake split across chunk boundaries mid-line; `MPDBG-READY`
  appearing as a substring inside another line (must match at line start only);
  trailing REPL echo/prompt bytes after the JSON on serial.

## Revalidation (2026-08-05, phase entry at HEAD 1a19be6)

Anchors re-resolved (micropython `38a903b8de20`): the emit side is now
`launcher/mpy_launch_debugpy.py:100-101`, `_detect_host` is `:37`,
`_do_execbuffer` is `commands.py:490`, and the harness parser is
`tests/fixtures/debuggee.py:252` `read_mpdbg_ready` (it moved out of
`test_epic1_foundations.py` since the ticket was written).

**The stated dependency on s5.3 is stale, and the roadmap's ordering is
right.** The ticket lists "s5.3 (unix flow — first consumer)", but the
execution sequence puts 5.4 at step 8 and 5.3 at step 9, with 5.4's needs as
5.1+2.2 — both done. More to the point, the duplication this story exists to
remove already exists in two places, so the interface is grounded in real
callers rather than an imagined one:

- `micropython/tools/mpremote/mpremote/commands.py:580` `_read_mpdbg_ready` —
  the serial raw-REPL reader added by s5.1, including the two-channel `\x04`
  exception draining and the pty timeout handling.
- `tests/fixtures/debuggee.py:252` `read_mpdbg_ready` — the subprocess-stdout
  reader, which also has to start from the text the fixture drained at startup.

s5.3's unix flow becomes the third caller. Proceed now.

**Q8 made the endpoint load-bearing, which raises the stakes on `0.0.0.0`.**
When the ticket was written nothing consumed the endpoint, so a `0.0.0.0`
payload was harmless. Now `mpremote debug` prints it as the address to attach
to, and s5.1's review already flagged that a board with no network interface
reports the wildcard and there is no route to it — currently documented in
`docs/reference/mpremote.rst` as a caveat rather than handled. That makes this
ticket's resolution matrix (unix → `127.0.0.1`; device reporting `0.0.0.0`
with no known address → hard error; real address verbatim) the actual fix for
a shipped wart, not a precaution.

The open question about emitting `host: null` instead of `"0.0.0.0"` stays
deferred to STORY-8.2 as written: the resolution rules above make the
launcher's contract adequate, and changing the emit side now would touch the
shipped script and the EPIC-1 tests for no behavioural gain.

## Execution progress

**COMPLETE (2026-08-06, commit e5cf83d).** `mpremote_debug_handshake`
registered; pin `38a903b8de20` -> `2ba7cf660087`. `mpdebug_handshake.py` holds
the one parser; both prior readers (`commands.py`'s serial one and
`tests/fixtures/debuggee.py`'s stdout one) call it. The record is
`{kind, host, port, caps, raw_host}` — `kind` so a future serial data plane
(SPIKE-2.2) needs no caller change, `raw_host` for diagnostics. Suite 239
passed / 1 xfailed.

Three rounds. The review's blocking finding was a real regression the tests
had not caught: unifying the two readers reordered the loop so the raw-REPL
`\x04` exception drain no longer fired on a realistic stream, which would have
returned s5.1's "device dies on `import debugpy` and reports nothing useful"
bug. The synthetic streams the new tests fed it were too tidy to expose that.

**The endpoint resolution turned out to be worth more than the ticket
expected.** Implemented as written, `do_debug` hard-errored on every real
pty run, because the unix build has no `network` module and so always reports
the wildcard — which left the suite with no end-to-end success path at all,
only fake transports with a fabricated address. A pty peer is a local process
by construction, and `is_pty` is already tracked on the transport (from s5.1's
read_until timeout fix), so a wildcard reported over a pty resolves to
loopback. That is both correct and what gives `mpremote debug` a genuine
success path: the device reports `0.0.0.0:<port>`, mpremote prints
`127.0.0.1:<port>`, and the pty test now connects to *what mpremote reported*
and completes a DAP `initialize` against it. The suite also dropped from ~160s
to ~43s, since those tests no longer wait out timeouts.

`_is_wildcard` normalises before comparing, so `"::0"`, `"[::]"` and trailing
whitespace are recognised; the literal-allowlist version passed them through
as connectable addresses.

Carried forward, not blocking: echoed device output is no longer byte-exact
(the shared reader decodes with `errors="replace"` to handle multibyte splits,
then re-encodes, so non-UTF-8 program output is mangled where mpremote's
`do_exec` passes raw bytes); the two-lines-is-an-error contract is only
enforceable on the subprocess source, since the serial reader yields one line
per poll and a test currently pins that absence; endpoint resolution runs
before the `requires` check, so on a board with no address the less actionable
error wins, and a failed run leaves the device blocked in `wait_for_client()`
with a bound socket so a retry hits "Already listening"; and the middle arm of
the matrix (device + known address) has no production caller until a
network-kind transport exists.

## Workflow shape

Runs inside the EPIC-5 phase workflow. Implementation: sonnet. Tests: haiku —
synthetic-stream unit tests cover most of it host-side. Review: opus standard +
adversarial (adversarial focus: the 0.0.0.0 resolution matrix and chunk-boundary
parsing), looped until clean.

## Open questions

- Whether the launcher should also be changed to report a sentinel like
  `host: null` instead of `"0.0.0.0"` when it has no address (clearer contract) —
  that touches STORY-1.4's shipped script and the EPIC-1 tests; if taken, do it as
  a coordinated micropython-lib change under STORY-8.2, not silently here.
