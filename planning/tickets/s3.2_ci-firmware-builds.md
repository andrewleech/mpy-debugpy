# CI: reproducible debug-firmware builds published to GitHub Releases

Phase: EPIC-3 / STORY-3.2
Depends on: STORY-3.1 (manifest — DONE); integration branches published (DONE, D5)
Written: 2026-07-06 at HEAD 59fab8450a (micropython 18c76f7c56, micropython-lib 0d42fcd5bc)
Revalidated: 2026-07-15 at HEAD 02fbe2b (micropython 6863e938a1, micropython-lib 5f74950e9e) -
  Q7 DECIDED (push `main` as work lands): the Actions/Release path is unblocked
  and `origin/main` is current. All firmware flag anchors re-verified unchanged
  at the recomposed 6863e938a1 (unix mpconfigvariant.h enables, mpconfig.h
  device defaults :1840-1853, stm32 Makefile :117, rp2 CMakeLists :596, esp32
  esp32_common.cmake :264, PYBD_SF6 TEXT0_ADDR, ci.sh toolchain anchors).
  launcher/firmware.py grew s3.3's selection: cmd_verify moved to :118,
  cmd_fetch to :151, cmd_select new at :262. firmware.toml's four legacy
  entries are now `deprecated = true` with empty download_urls (s3.3), and no
  binary blobs remain in-tree - the delete-blobs constraint is already
  satisfied. Two structural changes from phase 0: (1) the composition is
  mbm-rebuildable, so committed flag enables CANNOT be hand-commits on the
  integration branch (force-reset on every rebuild) - they need a registered
  local-only composition branch; (2) the first upstream/master default-target
  rebase is assigned to this story as a deliberate second stage. Baseline
  correction: the unix-artifact gate is now 46 passed / 1 xfailed.

## Context
The four prebuilt artifacts recorded in `firmware/firmware.toml` are legacy
pre-recomposition binaries with drifted provenance (three don't even match the
submodule SHA they shipped alongside — see ROADMAP Status, STORY-3.1 note). Q2
DECIDED (2026-07-06): GitHub Actions **on this repo** builds debug firmware from
the pinned integration SHA and publishes to GitHub Releases; the v1 board set is
**unix, rpi_pico_w (RPI_PICO_W), PYBD_SF6, ESP32_GENERIC**. Rationale and lineage:
[[20260706_recomposition.md]], decisions D5/Q2 in ROADMAP.md.

## Scope
In scope: a GitHub Actions workflow in THIS repo that (a) checks out the repo with
submodules at the pinned SHAs, (b) builds mpy-cross + the four targets with the
debug flags, (c) computes sha256 per artifact, (d) publishes artifacts to a GitHub
Release keyed on the micropython submodule SHA, and (e) regenerates/updates the
`firmware/firmware.toml` entries (repo, source_commit, flags, sha256, download_url)
replacing the four legacy entries. Local dockerised reproduction path documented.
Out of scope: capability-driven variant selection (s3.3); docs (s3.4); publishing
from the micropython fork's own CI (rejected by Q2).

## Files and anchors
- `micropython/ports/unix/variants/standard/mpconfigvariant.h:30-42` — SETTRACE=1,
  LOCALNAMES=1, PERSIST=0 are already on for unix `standard` on the integration
  branch; the unix build needs NO extra flags (`make -C ports/unix VARIANT=standard`).
- `micropython/py/mpconfig.h:1840-1853` — device-port defaults: SETTRACE=0,
  LOCALNAMES=0, PERSIST=0. No rp2/stm32/esp32 port or board header enables them
  (grep verified) — the device builds must inject the flags.
- Flag injection per port:
  - stm32: `micropython/ports/stm32/Makefile:117` appends `$(CFLAGS_EXTRA)` to
    CFLAGS — `make BOARD=PYBD_SF6 CFLAGS_EXTRA="-DMICROPY_PY_SYS_SETTRACE=1 -DMICROPY_PY_SYS_SETTRACE_LOCALNAMES=1"` works today.
  - rp2: CMake port; `micropython/ports/rp2/CMakeLists.txt:596` consumes
    `${MICROPY_DEF_BOARD}` in `target_compile_definitions` — no CFLAGS_EXTRA path.
  - esp32: `micropython/ports/esp32/esp32_common.cmake:264` likewise consumes
    `${MICROPY_DEF_BOARD}`.
- CI toolchain precedent (copy, don't reinvent): `micropython/tools/ci.sh` —
  `ci_esp32_idf_setup` (`:225-240`, clones ESP-IDF at `$IDF_VER`);
  `.github/workflows/ports_esp32.yml:22-23` pins IDF `v5.3` (oldest) / `v5.5.2`
  (newest) — pick ONE and record it in the manifest; arm-none-eabi via apt
  (`ci.sh:22`) or the pinned ARM 14.3.rel1 toolchain (`ci.sh:512-521`).
- `firmware/firmware.toml` — header note (top of file) already declares the four
  legacy entries as capture-time provenance records to be replaced by this story.
- `launcher/firmware.py:151` `cmd_fetch` + `:41` `PLACEHOLDER_URL_PREFIXES` — the
  fetch path exists and refuses placeholder URLs; this story populates real
  `download_url`s so `fetch`/`verify` become end-to-end usable. Carried from
  s3.3's Execution outcome, to address here: fetch network errors surface as
  tracebacks (make them clean errors); `select` (`:262`) returns a pre-existing
  local artifact without re-verifying its sha256 — add the re-verification
  (cheap, closes the stale-artifact hole); `verify` with no args fails on the
  all-deprecated manifest (resolves itself when the new entries land).

## Design constraints
- Reproducibility beats convenience: an artifact must be rebuildable from
  (source_commit, recorded flags) alone. Prefer committing the flag enables to
  the board configs (guarded `#ifndef` blocks in each board's mpconfigboard.h,
  matching how unix does it in mpconfigvariant.h) over ephemeral make
  arguments. Post-phase-0 rule: such a commit must live on a NEW local-only
  fork branch registered in `mbm.toml` (mirroring `local_names_implementation`)
  and enter the integration via `mbm rebase --local` with a pinned target — a
  hand-commit on `mpy-debugpy` is force-reset away on the next rebuild. If
  make-arg injection is used instead, the exact invocation must be recorded in
  the manifest `build_flags`/notes.
- `MICROPY_PY_SYS_SETTRACE_LOCALNAMES_PERSIST` stays 0 everywhere — it corrupts
  line numbers for all compiled code ([[20260706_recomposition.md]]).
- Manifest capability fields are build intent, never behaviour claims — the
  runtime probe (`debugpy/server/debug_session.py:82 probe_capabilities`) is the
  source of truth (D4; firmware.toml header).
- Local docker builds use the host-uid/path convention:
  `docker run --rm -v "$(pwd):$(pwd)" -w "$(pwd)" --user "$(id -u):$(id -g)" <image> <cmd>`.
- Repo must stop carrying binaries in-tree (STORY-3.1 acceptance criterion still
  open) — this story deletes `firmware/<id>/` blobs once Releases exist.

## Approach sketch
Staged (revalidation 2026-07-15), so CI iteration never couples with moving pins:

1. **Compose the flag enables.** New local-only fork branch on
   `andrewleech/micropython` (e.g. `debug_board_flags`) with guarded `#ifndef`
   SETTRACE+LOCALNAMES enables in the three device boards' mpconfigboard.h
   (PERSIST untouched at 0). Push to the fork, register as a third micropython
   entry in `mbm.toml`, recompose with the pinned target (equivalence check:
   update-vs-integration diff is exactly the boards change), force-move +
   push, reset side-effect branches, bump the submodule pin here, `make test`
   green.
2. **Local dockerised builds first.** Prove all four targets build from the
   new pin locally (host-uid/path docker convention; espressif/idf image for
   esp32, ARM GCC for rp2/stm32) before any CI YAML exists — record artifact
   paths, sha256s, toolchain versions.
3. **CI workflow.** One workflow, matrix over the four targets. unix: plain
   ubuntu runner, artifact = `ports/unix/build-standard/micropython` (ELF).
   rp2: cmake, artifact = `build-RPI_PICO_W/firmware.uf2`. stm32 PYBD_SF6:
   arm-none-eabi build, artifact is the dfu (`TEXT0_ADDR = 0x08008000`,
   `boards/PYBD_SF6/mpconfigboard.mk:7`) — confirm whether PYBD deployment
   expects mboot-wrapped dfu and name the artifact accordingly. esp32:
   espressif IDF (docker image `espressif/idf:<pinned>` is the cleanest
   runner-independent route), artifact = `build-ESP32_GENERIC/firmware.bin`.
   Iterate on a short-lived branch (Q7); land on `main` once green.
4. **Release + manifest.** A final job assembles the Release (tag =
   `fw-<micropython-short-sha>`) from `{artifact, artifact.sha256}` uploads
   and emits the manifest fragment; commit the `firmware.toml` update with the
   real entries. Decide the s3.3 leftover here: drop the four `deprecated`
   legacy entries (leaning: yes — the header note plus git history suffice).
5. **Deliberate master bump (stage 2, after CI is green on current pins).**
   First default-target `mbm rebase --local` onto current `upstream/master`
   (phase-0 carried-forward item): moves every pin, re-runs the firmware test
   suites and `make test`, then re-runs the CI pipeline to a second Release —
   which is the re-runnability acceptance proof.

Makefile gets a `firmware-fetch` convenience target later (s3.3 territory —
don't build selection logic here).

## Acceptance criteria and tests
- [x] all four artifacts build in CI from the pinned integration SHA; workflow is
  re-runnable and yields identical sha256 for unix (bit-reproducibility for
  cross-compiled device builds is best-effort — record toolchain versions).
- [x] Release assets exist; `uv run launcher/firmware.py fetch <id>` downloads and
  hash-verifies each; `verify` passes; a corrupted download fails loudly
  (adversarial: truncate the file, expect FAIL with both hashes printed).
- [x] `firmware/firmware.toml` legacy entries replaced; no binary blobs in-tree.
- [x] unix artifact runs `make test` green (46 passed / 1 xfailed baseline).
- [x] `select` re-verifies an existing local artifact's sha256; `fetch` network
  errors are clean messages, not tracebacks (s3.3 carried notes).
- [ ] device artifacts boot on real hardware (defer flash-test to STORY-6.4 if no
  hardware in CI; record which boards were bench-checked).

## Execution progress

**Stage 1 DONE (2026-07-15, commit 62c9103).** `debug_board_flags`
(`2487e69eba` on the fork) enables SETTRACE+LOCALNAMES on the three device
boards; registered in `mbm.toml`, recomposed with pinned target `13303f8d29`,
integration tip `6863e938a1` -> `7eb40f42ab` (tree diff exactly the three
headers), both refs fork-pushed, `make test` 46/1 first try. Standard +
adversarial reviews clean (nits only). Artifacts proven locally with settrace
symbols in the ELFs: PYBD_SF6 `firmware.dfu` (arm-gcc 13.2.1/ubuntu 24.04),
ESP32_GENERIC `firmware.bin` (espressif/idf:v5.5.1 — sandbox could not pull
v5.3; CI must still pin v5.3 or record a dated pin decision), unix re-verified
(live probe + `misc/sys_settrace*.py` suite). rp2 build did not complete in
the workflow run (local docker issue); the retry then surfaced a REAL firmware
bug — `py/profile.c` `frame_f_locals` calls `snprintf` with no
`#include <stdio.h>`, which unix/stm32/esp32 mask via their port include
chains and rp2's `-Werror` build exposes. Fixed 2026-07-15 (commit a210ef4):
fixup into the introducing commit on `local_names_implementation` (branch
stays bisect-clean, `7ae5f769c` -> `b67cec9f6`, PR #5 refreshed), recomposed
with the pinned target (tree diff exactly the two include lines,
`7eb40f42ab` -> `1f70f60684`), suite 46/1. The stage-1 stm32/esp32 artifact
sha256s above are therefore stale (built at `7eb40f42ab`); CI builds from the
final pin regardless.

**Stage 2 (local builds) COMPLETE (2026-07-15).** rp2 RPI_PICO_W built at
`1f70f60684` with settrace symbols and the pinned-commit banner verified
(uf2 sha256 `6f31097b...`); all four targets now prove locally. CI toolchain
lesson: the rp2 job needs `libstdc++-arm-none-eabi-newlib` (+`-dev`) in
addition to `gcc-arm-none-eabi` — pico-sdk unconditionally compiles a C++
new/delete shim, and the bare gcc package's alternatives symlink for the C++
headers is not reliably registered. Flash headroom note: RPI_PICO_W with
settrace is at 75.9% of 1200 KB.

Learned, binding on later stages:

- mbm recreates every registered branch's commits with fresh hashes on every
  rebase (verified: identical tree/parent/author-date, committer-date +1s), so
  equivalence gates MUST be tree-diff based; commit-count/SHA gates are
  unsatisfiable.
- This rebuild hit a pdb_support/local_names conflict (py/profile.c,
  mpconfigvariant.h) that phase-0's run did not; resolved byte-identical to
  the prior integration tip and rerere-recorded, so future rebuilds replay it.
- Docker bind mounts are silently EMPTY on this dev machine's WSL distro —
  local builds use a docker cp in/out pattern (see the stm32/esp32 invocation
  records in the session log); GitHub runners are unaffected.
- unix toolchain record needs gcc/glibc versions pinned by the CI runner image
  for the sha256-reproducibility acceptance to be meaningful (review nit).

**Stage 3 (CI workflow + manifest assembler) DONE (2026-08-04, commit
913f239, hardened through a5a5a6f and this round-2 fix pass).**
`.github/workflows/firmware.yml` builds the four v1 targets and gates
publishing behind `workflow_dispatch` on `main`;
`.github/scripts/write_fragment.py` + `launcher/gen_manifest.py` turn each
job's build evidence into a `[[variant]]` fragment and assemble them into
`manifest-fragments.toml`. Round-1 review found 10 mustFix items (all closed
in a5a5a6f: reproducibility (SOURCE_DATE_EPOCH, `.g<sha>` banner), safety
(exec bit, truncated-download guard, `..`-escape rejection), and provenance
(toolchain table, esp32 digest pin, release-clobber guard, fragment/asset
cross-check)) plus 16 nits. Round 2 re-reviewed and found the cross-check gate
still passable with an empty `artifact_sha256` (finding 1), all four
fragments hardcoding `settrace`/`save_names` with no build-evidence gate for
the three device ports (finding 2), the release job vulnerable to
cross-event `concurrency` cancellation (finding 3), and two items the round-1
fix pass missed (findings 4/5, below). All five closed in this pass:

1. `validate_fragment` now rejects an empty/missing `artifact_sha256`
   outright (not just a malformed one); the cross-check step uses
   `sha256sum --strict -c`; `gen_manifest.py` gained `--expect-ids`, wired
   into both the `--check` and assemble steps in the release job so a
   fragment set short of (or beyond) the four v1 ids fails the gate.
2. `.github/scripts/verify_capabilities.py` (new) asserts
   `MICROPY_PY_SYS_SETTRACE`/`_LOCALNAMES` actually resolved to `1` in the
   compiled build - via `compile_commands.json` + a `-dM` re-preprocess for
   the CMake ports (rp2, esp32) and the `%.pp` pattern rule already in
   `py/mkrules.mk` for the classic-Make ports (unix, stm32) - before
   `write_fragment.py` will write a fragment; `--capability settrace=... `
   and `--capability save_names=...` are now required arguments (no
   hardcoded default), so an mbm rebase that silently drops the
   `debug_board_flags` branch fails the build instead of shipping an
   overclaiming manifest.
3. The workflow's `concurrency` group is now keyed by `github.event_name` as
   well as `github.ref`, with `cancel-in-progress` false for
   `workflow_dispatch`: a `push` iterating the same ref can no longer cancel
   an in-progress Release publish (or vice versa); same-event runs still
   pre-empt each other for fast branch iteration.
4. `ci-firmware` dropped from the `push` trigger; `.github/scripts/**`,
   `tests/**`, `micropython-lib` added to the `paths` filter.
5. This entry.

Nits: of the 16 from round 1, 15 are fixed in this pass (`--target
"$GITHUB_SHA"` on `gh release create`; `needs.setup.outputs.micropython_repo`
/`micropython_sha` passed via job-level `env:` instead of inline
interpolation in every build job; firmware/fragment upload steps in the unix
job moved after the test gate; `Hash artifact` now hashes the staged
`out/...` copy in all four jobs; the unix test retry now diffs `--lf`
against the one known flake instead of blanket-retrying any failure; the
Makefile's `test` target uses `uv run --extra test` (uv.lock-pinned) instead
of unpinned `--with` packages; the 3 F401s in `test_firmware_fetch.py` and
the import-order/unused-noqa issues in every file this pass touched are
ruff-clean; `temp_firmware` and the variant-dict builder are deduped into
`conftest.py`/`helpers.py`; `load_manifest()` resolves `MANIFEST_PATH` at
call time so the fixture's monkeypatch isn't inert; `verify` with no args
now skips deprecated entries and unfetched artifacts instead of failing red,
while an explicitly-named variant id still fails hard if missing;
`gen_manifest.py`'s `assemble()` loads fragments once instead of twice and
`load_fragments`'s return annotation matches what it returns). Deferred: a
repo-wide ruff sweep (RUF059 unused-unpack, RUF100 non-enabled-`noqa:E402`)
across the pre-existing test suite - both patterns predate this branch, are
consistent across ~10 files, and are a ruff-config/style decision (whether to
enable `E402`, whether to rename unused unpacked results) orthogonal to
firmware CI; left for a dedicated lint-cleanup pass.

Round 3 found the retry guard's single-flake allowlist wrong for the suite's
actual flake set (four DAP test-timing races observed across repeated local
`make test` runs, not one - a second co-occurring flake defeated the guard's
string-equality check) and the exec-bit fix from round 1 covering only the
fresh-download path (an already-present, hash-valid, non-executable unix
artifact was reported "verified" by both `fetch` and `select`). Both closed:
`firmware.yml`'s guard now checks each `--lf`-collected failing id against a
`known_flakes` array (all four ids) instead of one line; `firmware.py` gained
`_ensure_executable()`, called from `_download_and_verify`, `cmd_fetch`'s
already-present branch, and `_resolve_artifact`'s existing-file branch.

Round 4 (adversarial) refuted the round-3 guard: three of the four allowlist
ids are parametrised tests whose collectable node ids carry a
parametrisation suffix embedding a machine-local absolute path, so full-id
equality could never match on a runner and the retry would never fire.
Closed: ids are compared with the suffix stripped (`"${id%%[*}"`), and the
allowlist is self-guarded - an entry matching no collectable test fails the
job loudly, so a test rename cannot silently disable its retry. The
recurring upload-artifact nit is closed too (`overwrite: true` on all eight
upload steps, so a full-run re-run after a partial failure cannot hit the
same-name conflict). Remaining recorded nits: parametrised node ids
embedding absolute paths (give `source_file` params an `ids=` label - fold
into the s5.5 harness pass), the republish path's `gh release edit` not
retargeting the tag, cmd_fetch's already-present branch verify/chmod outside
the OSError handler, urlopen without a timeout, deprecated-entry corruption
invisible to `verify`, and the esp32 digest literal repeated in three
places.

**Stages 3+4 (CI workflow + first Release) COMPLETE (2026-08-04, commits
6238506/b72797d/eb7f3fa on main, manifest at 7a08b85).** All four build jobs
went green on the FIRST push of `.github/workflows/firmware.yml` (unix 89s
incl. host DAP gate, stm32 176s, esp32 189s, rp2 191s); four review rounds
(std + adversarial opus, sonnet fixers) then hardened it - full log in the
rounds above. First dispatch on main published Release `fw-1f70f60684`
(target eb7f3fa): four firmware assets + checksums.txt +
manifest-fragments.toml, fragment sha256s cross-checked against the uploaded
bytes in-job. Reproducibility PROVEN: unix sha256
`ce1038367f8b...c0b262271` identical across two independent runs
(30906579347 branch-dispatch, 30907011385 main-dispatch) - SOURCE_DATE_EPOCH
pinning works. End-to-end verified from the live Release: `fetch` all four
(hash-verified, unix execute bit restored), `verify` all OK, `select --need
settrace,save_names --port unix` resolves, and the FETCHED unix binary runs
the host suite green (82 passed / 1 xfailed final baseline; suite grew 46 ->
82 across this story). firmware.toml now carries exactly the four CI entries
with toolchain + ci_run provenance; legacy entries dropped; `.dfu` added to
the fetched-blob gitignore set. The iteration branch is deleted; the
workflow's push trigger covers main only.

**Stage 5 (master bump, second Release) COMPLETE (2026-08-04).**
`mbm rebase --local` onto `upstream/master` landed micropython at
`f9d7c96b96` (from `upstream/master` `06bcfd5b74`, ~690 commits) and
micropython-lib at `00d364e7fb` (from `dbb3b45fde`); pin bump committed as
`e703924`. unix: settrace suites 11/11
(1149 testcases), host `make test` 82 passed/1 xfailed (one retry for the
known DAP-interleaving flake). Re-running `make test` a further 4 times
locally to measure the flake rate behind the CI retry gate: 2 of 4 runs red,
each on a different known_flakes id
(`test_dbg_06_evaluate.py::test_evaluate_repl_statement_mutates_running_global`
then, on the single permitted retry, `test_epic1_foundations.py::test_epic1_readonly_locals_hint`),
2 clean. At that rate a single retry has roughly a 1-in-4 chance of leaving
the gate red on an all-known-flake failure, so `.github/workflows/firmware.yml`
now retries up to 3 times (4 attempts total) instead of once, still failing
immediately on any id outside known_flakes. `extmod/select_poll_fd.py` was misdiagnosed as
a baseline failure carried from phase-0 in an earlier pass of this record;
corrected below and in `20260715_phase0-canonical-branches-mbm.md` - it needs
`ulimit -n 1024` (`tools/ci.sh:10`) to reach its `EINVAL` assertion, unrelated
to the master bump; re-run under that ulimit on this same build: 1 test
performed (5 testcases), pass.

Local device builds (docker cp pattern, same images as stage 2:
`mpy-rp2-build:ubuntu24.04`, `mpy-stm32-build:ubuntu24.04`,
`espressif/idf:v5.5.1`) proved all three device targets still build clean at
the bumped pin, each gated through `verify_capabilities.py` before trusting
the artifact:
- rp2 RPI_PICO_W: builds, FLASH 75.96% of 1200 KB (matches the 75.9%
  recorded at stage 2); `MICROPY_PY_SYS_SETTRACE`/`_LOCALNAMES` both resolve
  to 1 via `compile_commands.json`. uf2 sha256
  `6c665d5b6af1ebb4b02369cdc3c3883e716e6296cf052ff7a85d985893d2b12e`.
- stm32 PYBD_SF6: builds clean; capabilities verified via the `.pp`
  preprocess route. dfu sha256
  `eda9e408b8892895ada418721e47abcff84c9896b6679080795a872b3fd18181`.
- esp32 ESP32_GENERIC (espressif/idf:v5.5.1, same substitution as stage 1 -
  the sandbox still can't pull v5.3): builds clean, 14% app-partition
  headroom; capabilities verified via `compile_commands.json`. bin sha256
  `0e0797132ad5c2c9c7915b078557f13d62814d554b389bdd7f33b7829ee77161`.

Second Release proved re-runnability: dispatch run `30912616619` published
`fw-f9d7c96b96` (target `7708df6`, six assets), and the concurrent
push-triggered run `30912617595` built the same pin independently - **all
four artifacts byte-identical across the two runs** (the first Release had
only proven unix). The local device builds above do NOT match the CI hashes
(different toolchain images: idf v5.5.1 vs v5.3, host cross-gcc versions) -
expected, and why the manifest records the toolchain tables. Manifest
updated to the fw-f9d7c96b96 entries; e2e re-proof: fetch all four
(hash mismatch on cached old artifacts detected, re-fetched, verified),
`verify` all OK, select resolves, fetched unix binary runs the suite
82 passed / 1 xfailed. One full-suite run first failed 2 tests including
`test_epic1_foundations.py::test_epic1_breakpoint_stops_target`, which is
NOT in the known_flakes allowlist - same interleaving mechanism (passes in
isolation, 2.36s), first evidence the flake family extends beyond the four
allowlisted ids; raises the priority of the s5.5 harness fix (risk
register). Historical submodule pins are preserved as `mpy-debugpy-pin-<sha12>`
tags on both forks (mbm force-pushes orphan old tips; every pin in top-repo
history is now durably reachable). Device boot-check stays deferred to
STORY-6.4 per the acceptance note (no boards bench-checked yet).

## Workflow shape
Implementation on sonnet (workflow YAML + manifest regeneration script); test
authoring/running on haiku (fetch/verify/corruption tests against a local HTTP
stub); standard + adversarial review on opus (supply-chain angle: pinned actions,
pinned toolchains, no unpinned `latest`). Looped until reviews clean.

## Open questions
- PYBD_SF6 artifact format: ANSWERED (2026-07-15, stage 1) — the raw
  application `firmware.dfu` (TEXT0_ADDR 0x08008000; the build itself prints
  "requires mboot to be installed first", and `mboot/README.md` documents this
  address convention). Release asset is the plain dfu; the manifest must state
  mboot must already be resident (true for stock PYBD hardware) and that the
  artifact does not include mboot.
- Flag injection: RESOLVED at revalidation (2026-07-15) — committed board-config
  enables on a registered local-only composition branch (Approach step 1); the
  `MICROPY_DEF_BOARD`/`CFLAGS_EXTRA` make-arg route is the fallback only if a
  device board fails to build with the flags committed.
- IDF version pin: pick from the upstream matrix in
  `.github/workflows/ports_esp32.yml` at implementation time (upstream "oldest"
  = most compatible) and record it in the manifest.
- Whether the `deprecated` legacy manifest entries are dropped when the real
  entries land (s3.3 leftover, decided in Approach step 4 — leaning yes).
